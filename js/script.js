new Swiper( '.slider', {
	// direction: 'vertical', - сделать вертикальную прокрутку!

	// 🔧 ОСНОВНЫЕ НАСТРОЙКИ:

	// Скорость анимации переключения слайдов (в миллисекундах)
	speed: 2400,
	// 2400ms = 2.4 секунды - довольно медленная анимация
	// Создает плавный, кинематографический эффект

	// 📏 РАССТОЯНИЕ МЕЖДУ СЛАЙДАМИ:
	spaceBetween: 18,
	// 18px свободного пространства между слайдами
	// Видно при переключении, создает эффект разделения

	// 🎭 ПАРАЛЛАКС-ЭФФЕКТ:
	parallax: true,
	// Включает параллакс-эффект для элементов с data-swiper-parallax
	// Работает с HTML из предыдущего примера

	// 🔄 РЕЖИМ СВОБОДНОЙ ПРОКРУТКИ:
	// freeMode: true - свободная прокрутка
	// ЗАКОММЕНТИРОВАНО - сейчас отключено
	// Если раскомментировать:
	// - Слайды не будут "прилипать" к позициям
	// - Можно останавливаться между слайдами
	// - Более свободное, инерционное поведение

	// 🖱️ НАСТРОЙКИ КОЛЕСА МЫШИ:
	mousewheel: {
		enabled: true,      // Включить прокрутку колесом мыши
		sensitivity: 2.4    // Чувствительность прокрутки
		// Значение 2.4 = высокая чувствительность
		// Меньше 1 = низкая чувствительность, больше 1 = высокая
	},




	// 🧭 НАВИГАЦИЯ:
	navigation: {
		nextEl: '.swiper-button-next',
		prevEl: '.swiper-button-prev',
	},

	// 🔘 ПАГИНАЦИЯ:
	pagination: {
		el: '.swiper-pagination',
		clickable: true,

		// Адаптивность для пагинации
		dynamicBullets: window.innerWidth < 768 ? true : false,

		// Дополнительные адаптивные настройки пагинации
		dynamicMainBullets: window.innerWidth < 480 ? 1 : 3,

		// Можно добавить разные типы пагинации для разных устройств
		type: window.innerWidth < 768 ? 'bullets' : 'bullets'
	},




	// 📱 АДАПТИВНЫЕ НАСТРОЙКИ:
	breakpoints: {
		// Когда ширина окна >= 320px
		320: {
			speed: 1200,
			spaceBetween: 8,
			mousewheel: {
				sensitivity: 1.2
			}
		},
		// Когда ширина окна >= 768px
		768: {
			speed: 1600,
			spaceBetween: 12,
			mousewheel: {
				sensitivity: 1.8
			}
		},
		// Когда ширина окна >= 1024px
		1024: {
			speed: 2000,
			spaceBetween: 16,
			mousewheel: {
				sensitivity: 2.2
			}
		},
		// Когда ширина окна >= 1440px
		1440: {
			speed: 2400,
			spaceBetween: 18,
			mousewheel: {
				sensitivity: 2.4
			}
		}
	},

	// 📱 ДОПОЛНИТЕЛЬНЫЕ АДАПТИВНЫЕ НАСТРОЙКИ:
	resizeObserver: true, // Следить за изменением размера контейнера

	// 🎯 ОПТИМИЗАЦИЯ ДЛЯ МОБИЛЬНЫХ:
	touchRatio: 0.8, // Чувствительность тач-свайпа на мобильных
	touchAngle: 45,  // Угол для вертикального свайпа
	simulateTouch: true, // Имитация тача на десктопе
	shortSwipes: true, // Короткие свайпы
	longSwipes: false, // Отключаем длинные свайпы

	// 📦 ДОПОЛНИТЕЛЬНЫЕ ОПЦИИ ДЛЯ ПЛАВНОСТИ:
	grabCursor: false, // Показывать "руку" при наведении
	watchSlidesProgress: true, // Следить за прогрессом слайдов (нужно для параллакса)

	// 🔄 ЕСЛИ ЗАХОТИТЕ ВКЛЮЧИТЬ СВОБОДНЫЙ РЕЖИМ С АДАПТИВНОСТЬЮ:
	// freeMode: {
	// 	enabled: true,
	// 	sticky: true, // Слайды "прилипают" в конечных позициях
	// 	momentum: true, // Инерция
	// 	momentumBounce: true, // Отскок
	// 	momentumRatio: 1, // Соотношение инерции
	// 	momentumVelocityRatio: 1 // Соотношение скорости
	// }
} );





